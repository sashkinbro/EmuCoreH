*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Build outputs
build/
app/release/
app/.cxx/
core-android/.cxx/
core/build/
.kotlin/

# Local-only tooling and private data (IGDB credentials, catalog builds, scripts)
/IGDB
/public
/tools

# Signing material
*.jks
*.keystore

# Local Firebase configuration and hosting content (never commit)
/.firebaserc
/firebase.json
/firestore.rules
/firestore.indexes.json
/.firebase/
firebase-debug.log*
firestore-debug.log*
/y/

# Local Firebase app configuration (contains project API keys)
app/google-services.json
